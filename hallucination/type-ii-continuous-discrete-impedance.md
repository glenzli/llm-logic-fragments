# II. 连续-离散阻抗失配 (Continuous-Discrete Impedance Mismatch)

**分类坐标**：架构层 × 确定性任务（补充机制）

## 形式化

刚性逻辑的运算域是离散空间 $\{0, 1\}^n$ 上的布尔代数。Transformer 的实际运算域是连续空间 $\mathbb{R}^d$ 上的浮点运算与非线性激活。

将离散逻辑步骤映射到连续激活函数 $\sigma$ 时，每一步引入量化噪声 $\delta_i$（来源于浮点舍入和非线性拟合残差）。在 $k$ 步逻辑链中，累积误差的行为取决于任务的 Lipschitz 常数 $\mathcal{L}$：

$$
\|\text{err}_k\| \leq \sum_{i=1}^{k} \mathcal{L}^{k-i} \cdot \delta_i
$$

当 $\mathcal{L} > 1$（逻辑链对中间状态敏感）时，误差指数增长。对于离散判定问题，存在一个临界步数 $k^*$，超过该步数后累积误差穿越决策边界，导致输出翻转（即幻觉）：

$$
\exists\, k^* \in \mathbb{Z}^+,\quad \forall k > k^*: \quad \text{sgn}(\mathcal{T}(x)_k) \neq \text{sgn}(f(x))
$$

## 佐证

上述形式化的核心断言是：每步量化噪声 $\delta_i > 0$ 不可消除。一旦接受这个前提，累积误差公式和临界步数 $k^*$ 的存在性是直接的数学推论。因此，佐证的重点在于证明 **"$\delta_i > 0$ 不可消除"这一根因**。

### 数学论证：为什么 $\delta_i > 0$ 不可消除

以下论证分为**严格成立的数学定理**和**高度可信但未完全形式化的推论**。分界线已明确标注。

**定理 1（通用逼近定理，UAT）** [Hornik et al., 1989; Cybenko, 1989]：
设 $\sigma$ 为非常值连续激活函数。对任意紧致集 $K \subset \mathbb{R}^n$、任意连续函数 $f \in C(K)$、任意 $\epsilon > 0$，存在网络参数 $\theta$ 使得：

$$
\sup_{x \in K} |\mathcal{T}_{\theta}(x) - f(x)| < \epsilon
$$

但 UAT **不保证** $\epsilon = 0$ 的精确匹配，且逼近仅在紧致集 $K$ 上成立。

**命题 2（Transformer 输出函数的连续性）**：
Transformer $\mathcal{T}_\theta$ 由矩阵乘法、LayerNorm、Softmax、SiLU/GeLU 等运算复合而成，均为关于输入表征的连续函数。因此，对于任意两个输出类别 $i, j$ 的 logit 差函数：

$$
g_{ij}(z) = \text{logit}_i(z) - \text{logit}_j(z), \quad g_{ij}: \mathbb{R}^d \to \mathbb{R}
$$

$g_{ij}$ 是输入表征 $z$ 的**连续函数**。

**定理 3（介值定理推论——决策边界的必然存在）**：
若输入表征空间 $Z \subset \mathbb{R}^d$ 是连通的，且存在 $z_1, z_2 \in Z$ 使得 $g_{ij}(z_1) > 0$ 且 $g_{ij}(z_2) < 0$，则由连续函数的介值定理：

$$
\exists\, z^* \in Z, \quad g_{ij}(z^*) = 0
$$

即：**决策边界（等值面 $g_{ij}^{-1}(0)$）在输入表征空间中必然存在。** 决策边界的存在意味着：连续 logit 函数在仿真离散阶跃时，必然存在过渡区间而非完美跳变。这个过渡区间就是每步量化噪声 $\delta_i$ 的几何来源。

> [!CAUTION]
> **⚠️ 严格证明到此为止。** 以下推论在数学上高度可信，但尚未被形式化为一般性不可能定理。读者应注意区分。

**推论 4（单步噪声的不可消除性）** *[未严格证明]*：
由 $g_{ij}$ 的连续性，决策边界 $g_{ij}^{-1}(0)$ 的 $\eta$-邻域

$$
\mathcal{N}_\eta = \{z \in Z : |g_{ij}(z)| < \eta\}
$$

构成一个非空开集。当离散输入空间 $\mathcal{X}$ 足够大（如无限可数集），其 Embedding 像 $\phi(\mathcal{X}) \subset \mathbb{R}^d$ 足够稠密，必然存在某些输入 $x \in \mathcal{X}$ 满足 $\phi(x) \in \mathcal{N}_\eta$。对于这些输入，每步仿真的量化噪声 $\delta_i$ 不可被压缩到零。

> [!IMPORTANT]
> 推论 4 的关键跳跃在于：**从"连续空间中决策边界必然存在"到"离散输入中必然有点落入边界邻域"**，需要假设 Embedding 像的分布密度与决策边界的几何位置存在不可避免的交叉。这在高维空间中几乎必然成立（高维空间中低维流形的邻域体积占比极高），但这是几何概率论证，而非构造性证明。佐证级别：**合理性论证（plausibility argument）**，强于猜想，弱于完备证明。

**一旦接受 $\delta_i > 0$ 不可消除，则上述形式化中的累积误差公式直接给出结论**：当 $\mathcal{L} > 1$ 时，$\|\text{err}_k\|$ 随步数 $k$ 指数增长，临界步数 $k^*$ 的存在是算术必然——无需额外证明。

### 工程实验：长度泛化的系统性失败

长度泛化（Length Generalization）实验是该机制最直接的工程验证。标的函数为最简确定性函数——多位整数加法 $f(x_1, x_2) = x_1 + x_2$。

| 实验 | 发现 | 论文 |
|---|---|---|
| Grokking 顿悟现象 | 模型在训练集上先过拟合，后突然达到完美泛化——但仅限训练分布内 | [Power et al., 2022](https://arxiv.org/abs/2201.02177) |
| 简单算术的局限性 | Transformer 在分布内达到高准确率，但无法泛化到超出训练位数的加法 | [Nogueira et al., 2021](https://arxiv.org/abs/2102.13019) |
| 小型 Transformer 的算术学习 | 即使使用 CoT 数据增强，模型仍无法鲁棒地泛化到训练范围外的位数 | [Lee et al., 2023](https://arxiv.org/abs/2307.03381) |
| 长度泛化的条件性 | 特定位置编码可实现有限外推（如 5 位→15 位加法），但乘法彻底失败 | [Nogueira et al., 2023](https://arxiv.org/abs/2306.15400) |
| 长度泛化的脆弱性 | 即使找到可泛化的配置，成功高度依赖数据格式和随机初始化种子 | [Kazemnejad et al., 2024](https://arxiv.org/abs/2402.09371) |

**与形式化的对应关系**：整数加法中，输入位数 $n$ 直接决定了隐式推理链的步数 $k$——每一位的"逢十进一"判定是一个离散逻辑步骤，每个步骤在连续空间中仿真时引入量化噪声 $\delta_i$。位数增加 $\Leftrightarrow$ 推理链步数 $k$ 增加 $\Leftrightarrow$ 累积误差 $\|\text{err}_k\|$ 增长。实验中观察到的"位数超出训练范围后崩溃"，正是累积误差穿越临界步数 $k^*$ 的宏观表现。

## 结论

连续空间中的离散逻辑仿真是**条件稳定的**。短链推理可以工作；但链路长度超过架构和精度决定的临界阈值后，错误积累将不可避免地导致逻辑翻转。

## 缓解路径

**Tool Use**。将需要精确离散运算的步骤交给确定性执行器（代码解释器、符号求解器），避免在连续空间中长链仿真离散逻辑。
