# Part 01 — 后续章节草案备忘

> 本文件记录 Part 01 后续章节的规划方向，防止遗忘。

## CH2 当前状态（已定稿）

命题 2.1–2.12，含推论 2.5、2.9。覆盖：
- §2.1 分量分划（$I_{\mathrm{id}} / I_{\mathrm{const}} / I_{\mathrm{act}}$）
- §2.2 分量正交性（单向隔离、完全解耦等价性、链式阻断）
- §2.3 复合几何（左复合放缩、右复合收缩、双端隔离、全局漂移级联界、交换子界）
- §2.4 分量传递律（活跃包含律、恒等保留律、常值反渗与退化）

## CH3 宏观限界与可计算性分析（待启动）

基于 CH2 建立的算子代数工具，进入宏观系统层面的分析。

### 候选方向

1. **谱半径与迭代收敛**：Lip 矩阵连积 $\mathbf{L}^n$ 在 $n \to \infty$ 时的渐近行为。$\rho(\mathbf{L}) < 1$ 时的全局收缩性；$\rho(\mathbf{L}) > 1$ 时的发散判据。

2. **稳定性极限（Stability Limit）**：基于定理 2.8 的全局漂移方程，推导 IDFS 架构下大模型系统迁移的误差累积上界。当漂移超过临界阈值时，系统等价性破裂。

3. **Lip 矩阵的可计算/可估计条件**：在什么条件下，受限 Lip 矩阵 $\mathbf{L}\big|_S$ 可以从有限样本中可靠估计？这关系到理论的实际可操作性。

4. **不动点结构**：若 $\phi \circ \phi = \phi$（幂等算子），分量分划有何约束？与迭代收敛的关系。

5. **测度退化的不可逆性判据**：常值分量反渗（命题 2.12）在什么全局条件下是不可逆的？连积矩阵列零条件的可判定性。

---
*[IDFS] ⊢ [GLENZLI] ⊢ [Part 01] ⊢ [draft]*
